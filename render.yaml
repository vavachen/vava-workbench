services:
  - type: web
    name: vava-workbench
    runtime: node
    plan: free
    buildCommand: "true"
    startCommand: "node server/server.js"
    healthCheckPath: /api/health
    envVars:
      - key: PORT
        value: 10000
